# TC_001 — Shopping list
# Auto-generated by Nipachan v5
# Shared steps are loaded globally via pytest_plugins in conftest.py
# Shared steps used:
#   Given I am on the application home page  →  i_am_on_the_application_home_page
#   Then I verify "Shopping List" link is visible  →  i_verify_link_is_visible
#   When I search for "Cake"  →  i_search_for_value

from pytest_bdd import given, when, then, parsers, scenarios
from playwright.sync_api import Page

scenarios("../../TC_001_shopping_list.feature")

# ── New step definitions ──────────────────────────────────────

@then(parsers.parse('I verify "{text}" is visible in the first item name'))
def i_verify_value_is_visible_in_the_first_item_name(page: Page, text: str):
    from utils.web_actions import WebActions
    from pages.common_page import CommonPage
    wa = WebActions(page)
    locator = CommonPage.text_by_value(text)
    wa.wait_for_visible(locator)
